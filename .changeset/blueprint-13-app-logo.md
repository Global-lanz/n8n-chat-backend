---
"chat-n8n-backend": minor
---

Expõe o campo `appLogo` no endpoint público `/api/config`, lido da chave `app_logo` na tabela de settings. Permite que o frontend exiba a logo personalizada da aplicação sem autenticação.
